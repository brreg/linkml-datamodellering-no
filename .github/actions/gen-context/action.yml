name: Generer context.jsonld
description: Køyrer gen-jsonld-context for alle skjema i domenet

inputs:
  domain:
    description: Domenenamn (ap-no, fair, fint, ngr, oreg, samt)
    required: true

runs:
  using: composite
  steps:
    - name: Generer context.jsonld
      shell: bash
      run: make domain-gen-context DOMAIN=${{ inputs.domain }}
