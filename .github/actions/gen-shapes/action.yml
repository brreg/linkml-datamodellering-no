name: Generer shapes.ttl (SHACL)
description: Køyrer gen-shacl for alle skjema i domenet

inputs:
  domain:
    description: Domenenamn (ap-no, fair, fint, ngr, oreg, samt)
    required: true

runs:
  using: composite
  steps:
    - name: Generer shapes.ttl (SHACL)
      shell: bash
      run: make domain-gen-shapes DOMAIN=${{ inputs.domain }}
